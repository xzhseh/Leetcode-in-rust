pub struct Solution;

impl Solution {
    #[allow(dead_code)]
    pub fn num_islands(grid: Vec<Vec<char>>) -> i32 {

    }

    #[allow(dead_code)]
    pub fn dfs(current_grid: &mut Vec<Vec<char>>, col: usize, row: usize) {
        
    }
}